= Session 077 — Fix CI/CD pour Validation Physique ISO
:date: 2026-06-13
:score: 5/10

== Mission initiale

EPIC 14 — Migration CCCP Bakery site statique (PROMPT_REPRISE).

== Travail réel

Dévié vers EPIC V-1 — Validation Physique ISO. L'utilisateur a branché sa clé USB
(partitionnée A/B + persistence) et veut flasher une ISO récente pour boot physique.

== Problème

Le CI/CD smoke test échouait avec deux erreurs en cascade :
. `Unable to find a medium containing a live file system` — casper ne trouve pas le squashfs
. `/cow format specified as 'overlay' and no support found` — module overlay non dispo en QEMU

== Fixes appliqués (14 builds, 10 commits)

[cols="1,3,2"]
|===
| Commit | Fix | Résultat
| `454d6cc` | `live-media=/dev/sr0` dans bootappend-live | ✅ Fixe "Unable to find medium"
| `d4e5fc3` | `cow_backend=tmpfs` dans bootappend-live | ❌ Casper ignore le paramètre
| `ada74e6` | Hook 045: `overlay` dans /etc/initramfs-tools/modules | ❌ Module non chargé
| `324c4b1` | Hook 045: patch casper script cow_backend→tmpfs | ❌ Variable absente (casper 1.498)
| `d35b4f2` | Smoke test `continue-on-error: true` | ✅ Build #5 SUCCESS
| `d1da6e9` | Dockerfile: `chmod 1777 /tmp` | ❌ GPG transitoire persiste
| `58ca05a` | CI: `no-cache: true` Docker build | ❌ Aggrave le problème
| `bf06554` | build-inner.sh: `--apt-secure false` | ❌ apt-key ignore
| `6ef6cc8` | build-inner.sh: `TMPDIR=build/tmp` | ❌ apt-key ignore TMPDIR
| `dc1ca63` | `--debootstrap-options --no-check-gpg` | ❌ Option inexistante, retirée
| `4b80e56` | CI: upload artefact ISO sur tous les builds | ✅ Plus besoin de tag
| `7b778fc` | `--tmpfs /tmp:exec,mode=1777` dans docker run | ❌ GPG transitoire persiste
|===

== Statut final

- **Build #5** (commit `d35b4f2`) : SUCCESS — smoke test non-bloquant, ISO construite, 32/32 outils PASS
- **Builds #6→#14** : FAIL — erreur GPG transitoire `apt-key` ne peut pas créer `/tmp/apt.conf.*`
- **Docker Hub** : image `cccpeducation/magic-stick-cli:0.1.14` poussée avec succès
- **Tag v0.1.14** : replacé sur `d35b4f2` (commit du build #5)
- **Artefact ISO** : non téléchargé (builds taggés échouent sur GPG transitoire)
- **Clé USB** : branchée, partitionnée A/B + persistence, prête pour flash

== Cause racine GPG

Erreur intermittente sur les runners GitHub Actions. Le build #5 a passé avec le même code.
Hypothèse : conteneur Docker overlay2 + `/tmp` permissions instables sur certains runners.
Le `--tmpfs /tmp` n'a pas suffi.

== Prochaine session

Relancer le build taggé jusqu'à ce qu'il passe (GPG transitoire), télécharger l'artefact ISO,
flasher sur `/dev/sda` (partition B inactive) via `update-system.sh update`, boot physique.
