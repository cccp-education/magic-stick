#!/bin/sh
set -e

echo "==> Adding overlay kernel module to initramfs for casper cow support"
echo "overlay" >> /etc/initramfs-tools/modules
echo "  overlay module added to /etc/initramfs-tools/modules"
echo "  Will be included in initrd when live-build regenerates it."
