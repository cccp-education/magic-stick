= Backlog — Magic Stick
:last_update: 2026-06-15 (Session 082)

'''
**Objectif** : Suivi des issues et taches en cours
'''

== EPICs Terminés

=== EPIC 1 : Initialisation projet ✅
=== EPIC 3 : Partition A/B + Persistence ✅
=== EPIC 5 : Personnalisation XFCE ✅
=== EPIC 6 : Script de mise à jour système A/B ✅ (v0.3.0 dual boot UEFI+Legacy)
=== EPIC 8 : Dockerisation CLI (artefact parallèle) ✅
=== EPIC 9 : Éditeurs de code (VS Code, gh CLI) ✅
=== EPIC 11 : Pinning versions chroot + refactoring patterns ✅
=== EPIC 14 : Migration CCCP — Docker Hub + SourceForge ✅ (session 076)

== EPIC 2 : Build ISO Xubuntu de base 🔄 EN COURS

**Priorité** : HAUTE

=== Réalisé
* [x] live-build configuré, hooks chroot, ubiquity, QEMU+OVMF
* [x] CI/CD GitHub Actions (build + verify + test + release)
* [x] Première ISO générée (session 010)
* [x] Release v0.1.14 sur SourceForge
* [x] GRUB modules corrigés (session 013)
* [x] Fix casper overlay (cow_backend=tmpfs, overlay module initramfs)
* [x] Fix QEMU boot (patch casper script)
* [x] Support UEFI (hook 990-add-efi-support.binary, grubx64.efi + BOOTX64.EFI)
* [x] Support Legacy BIOS (bios_grub partition, core.img avec modules ext2/gfxterm)

=== Restant
* [ ] Tester boot BIOS sur clé USB réelle
* [ ] Tester boot UEFI sur clé USB réelle

== EPIC 4 : Logiciels (Ollama, Docker, SDKMAN, NVM, etc.) 🔄 EN COURS

**Priorité** : MOYENNE

=== Réalisé
* [x] Docker, Podman, Ollama, NVM, pnpm, Python 3.14.4, uv, SDKMAN+JDK25
* [x] ripgrep, fd, fzf, lazygit, xh, just, Flatpak, JetBrains Toolbox, zsh+starship
* [x] CI/CD test-software.sh checks binaires squashfs
* [x] Smoke tests QEMU (service systemd, mode --smoke, task Gradle isoTestSmoke)
* [x] CI/CD smoke tests validés

=== Restant
* [ ] Tester en live boot réel sur clé USB

== EPIC V-1 : Validation Physique ISO 🔄 EN COURS

**Priorité** : CRITIQUE

=== Réalisé
* [x] update-system.sh v0.3.0 : layout GPT 5 partitions (bios_grub + ESP + system_a + system_b + persistence)
* [x] GRUB dual boot Legacy (i386-pc) + UEFI (x86_64-efi) installé
* [x] Fix core.img sans ext2 → prompt GRUB (session 080)
* [x] Fix grub.cfg sortie série → gfxterm (session 080)
* [x] UEFI grubx64.efi avec modules embarqués (Secure Boot ready)
* [x] 6 tâches Gradle : isoSetupAB, isoCheckUSB, isoUpdateSystem, isoValidateUSB, isoTestPersistence, isoReleaseNotes
* [x] test-persistence-mock.sh + test-ab-partition.sh adaptés v0.3.0
* [x] Clé USB /dev/sda flashée : core.img 179 KB, grubx64.efi 2.3 MB, verify passe

=== Restant
* [ ] Boot physique sur machine réelle — Legacy BIOS
* [ ] Boot physique sur machine réelle — UEFI

== EPIC 7 : Site de doc auto + IA release notes

**Priorité** : BASSE
* [ ] Auto-updater site doc (version + lien SourceForge)
* [ ] Générer notes de version via IA lors d'un release

== EPIC 10 : Applications Docker supplémentaires

**Priorité** : BASSE
* [ ] BigBlueButton, Rahoot, Formbricks, Grist, Cal.com, Nextcloud, Stalwart
* [ ] Reverse proxy (Traefik/NPM)
* [ ] Script start-apps.sh + intégration live-build

== EPIC 12 : Vinegar + Sober (Roblox) 🔲 A FAIRE

**Priorité** : BASSE
* [ ] Installer Vinegar (Flatpak ou binaire)
* [ ] Installer Sober (Flatpak ou binaire)
* [ ] .desktop files + test boot

== EPIC 13 : Plateformes de jeu 🔲 A FAIRE

**Priorité** : BASSE
* [ ] Heroic Games Launcher, Prism Launcher, RetroArch, Itch.io

== EPIC 15 : Network Boot & Remote Provisioning 🔄 EN COURS

**Priorité** : MOYENNE
**Objectif** : Booter magic-stick en live sur des laptops Windows du réseau local
sans reformater, sans toucher leurs partitions, sans clé USB complète.
Pilotage à distance depuis la machine hôte magic-stick.

=== US-1 : Proxy DHCP + TFTP/HTTP (dnsmasq)
* [x] Installer et configurer dnsmasq en proxy DHCP (ne casse pas le DHCP de la box) — `scripts/network-boot.sh`
* [x] `dhcp-range=proxy` + `dhcp-option=tag:pxeclient,option:bootfile-name` — config générée par le script
* [x] Servir kernel + initrd via HTTP (Python http.server, plus rapide que TFTP) — `network-boot.sh` start_server
* [ ] Hook chroot pour inclure dnsmasq dans l'ISO

=== US-2 : iPXE bootloader réseau
* [ ] Compiler iPXE avec script embarqué (chaîne kernel+initrd HTTP + cmdline casper)
* [x] Servir iPXE via TFTP (dnsmasq `dhcp-boot=undionly.kpxe`) — script détecte ipxe-bootimgs ou build/undionly.kpxe
* [ ] Fallback : mini clé USB (50 Mo) avec juste iPXE → chaîne vers HTTP réseau
* [x] Script iPXE : `kernel http://<host>/vmlinuz`, `initrd`, `boot` — `magic-stick.ipxe` généré automatiquement

=== US-3 : Wake-on-LAN
* [x] Script `wake-laptops.sh` — envoie magic packet aux MACs des laptops cibles (etherwake + fallback Python)
* [ ] Découverte MACs via scan ARP ou fichier config `laptops.conf`
* [x] Intégration dans la tâche Gradle `isoProvisionNetwork` — build.gradle.kts

=== US-4 : SSH dans l'ISO live
* [ ] Hook chroot : installer openssh-server, générer clé hôte au boot
* [ ] Mot de passe ou clé SSH pré-partagée pour l'utilisateur `magic`
* [ ] Option kernel `ssh.enabled=1` dans le script iPXE
* [ ] Test : SSH depuis l'hôte vers le laptop booté en live

=== US-5 : Tâche Gradle `isoProvisionNetwork` — build.gradle.kts
* [x] Tâche `isoProvisionNetwork` : dnsmasq → wake-laptops → attendre boot → vérifier SSH → provisionner
* [x] Tâche `isoNetworkBoot` : démarre le serveur seul
* [x] Tâche `isoWakeLaptops` : envoie WoL aux MACs spécifiées
* [x] Tâche `isoNetworkTest` : placeholder QEMU test
* [x] Paramètres : `-Plaptops=mac1,mac2,mac3 -Piface=eth0 -PhttpPort=8080`
* [ ] Mode dry-run : affiche ce qui serait fait sans exécuter

=== US-6 : Test bout en bout
* [ ] Test QEMU : simuler 2 clients PXE + 1 serveur dnsmasq
* [ ] Test réel : 2 laptops Windows, boot réseau, SSH fonctionnel
* [ ] Documentation : `NETWORK_BOOT.adoc` dans `docs/`

== EPIC 16 : Virtual Appliance (VPS/QEMU/KVM) 🔄 EN COURS

**Priorité** : HAUTE
**Objectif** : Déployer magic-stick comme VM headless sur VPS/cloud
(Proxmox, KVM/libvirt, QEMU brut). Accès SSH + console série,
drivers virtio, image qcow2 prête à l'emploi.

=== US-1 : Hook chroot virtual appliance
* [x] Hook `055-virtual-appliance.chroot` : serial-getty@ttyS0, SSH host keys firstboot, virtio modules initramfs, headless detection script
* [x] Package `openssh-server` ajouté à `live.list.chroot`
* [x] `magic-stick-welcome.sh` patché pour mode headless (détection `virtual=true` cmdline + systemd-detect-virt + DISPLAY)
* [x] `magic-stick-welcome.desktop` wrapper avec détection headless

=== US-2 : GRUB headless serial entry
* [x] Entrée GRUB "Headless (serial console)" dans `990-add-efi-support.binary` : `nomodeset console=ttyS0,115200 virtual=true`, terminal_input/output serial

=== US-3 : ISOLINUX menu 4 modes BIOS
* [x] Fichiers custom `includes.binary/isolinux/` : live.cfg (Live + nomodeset + safe mode), install.cfg (Install), menu.cfg, stdmenu.cfg, splash.png
* [x] Menu ISOLINUX Legacy BIOS identique au GRUB UEFI (4 modes)

=== US-4 : Script + Gradle task qcow2
* [x] `scripts/virtual-appliance.sh` : qemu-img create qcow2 + boot ISO → disque virtuel
* [x] Tâche Gradle `isoVirtualAppliance` : génère qcow2 depuis l'ISO dans Docker

=== US-5 : Documentation
* [x] `docs/VIRTUAL_APPLIANCE.adoc` : guide déploiement QEMU/KVM/Proxmox/VPS
* [ ] Mettre à jour README pour mentionner le mode virtual appliance

=== US-6 : Test bout en bout
* [ ] Test QEMU headless : boot qcow2, vérifier SSH + serial-getty + virtio
* [ ] Tâche Gradle `isoTestVirtual` : test automatisé du scénario VPS dans QEMU
* [ ] Test réel : déploiement sur VPS (edster ou autre)
