= PROMPT REPRISE — magic-stick Session 083
:date: 2026-06-15

== Mission en cours

EPIC 16 Virtual Appliance (VPS/QEMU/KVM) — 60% done (US-1→5 faits, US-6 restant).
+ ISOLINUX 4 modes BIOS — fichiers créés, à vérifier au rebuild.

== Dernière action

Session 082 — Fix Cache Downloads + tentative rebuild ISO 0.1.15 :
- Fix cache downloads : `DL_CACHE` migré hors `build/` → `cache/downloads/` (persiste entre cycles Gradle)
- Fix doublon `open-infrastructure-system-config` dans `live.list.chroot`
- Patch live-config **confirmé fonctionnel** : hooks passent, conflit résolu
- Build ISO interrompu : disque saturé (chroot 12GB + cache 2.6GB > 21GB libres)
- Nettoyage Gradle caches anciens (~3.8GB) → 25GB libres
- `~/.local/share/opencode/` (4.9GB) préservé — données de session précieuses

== Bloqueur actif

*Aucun.* Patch live-config fonctionnel. Espace disque suffisant (25GB).
Le build ISO 0.1.15 est prêt à être relancé.

== A faire (Session 083)

. Rebuild ISO 0.1.15 → `./gradlew isoBuild` (timeout 120 min, ~30GB requis)
. Tester VNC : vérifier 4 modes ISOLINUX BIOS + entrée GRUB Headless serial
. EPIC 16 US-6 : Test QEMU headless (boot qcow2, SSH + serial-getty + virtio)
. EPIC 16 US-6 : Tâche Gradle isoTestVirtual
. Publier ISO 0.1.15 sur SourceForge
. Publier image Docker CLI sur DockerHub
. EPIC 15 US-4 : Hook chroot SSH (vérifier si déjà couvert par 055)

== Rappels

. ZERO commit/push sans permission
. Remote : cccp-education/magic-stick
. Clé USB : /dev/sda, 5 partitions, System A=v0.1.14, System B=vide
. sudo password dans `mdp.txt` (`issa`)
. Build ISO : 30-60 min, lancer avec timeout long
. Cache downloads : `cache/downloads/` (hors build/, persiste entre cycles)
. Espace disque minimum pour build : ~30GB
