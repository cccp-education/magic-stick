= PROMPT REPRISE — magic-stick Session 082
:date: 2026-06-14

== Mission en cours

EPIC 15 Network Boot & Remote Provisioning — ~25% done.
- US-1 : `scripts/network-boot.sh` — dnsmasq proxy DHCP + TFTP + Python HTTP (kernel/initrd)
- US-2 : `magic-stick.ipxe` généré auto — manque compilation iPXE + clé USB fallback
- US-3 : `scripts/wake-laptops.sh` — WoL (etherwake + Python fallback)
- US-5 : 4 tâches Gradle dans `build.gradle.kts`
- US-4 : Hook chroot SSH — à implémenter
- US-6 : Test QEMU + doc — à implémenter

== Dernière action

Session 081 — EPIC 15 Gradle tasks + scripts :
- `build.gradle.kts` : `isoNetworkBoot`, `isoWakeLaptops`, `isoProvisionNetwork`, `isoNetworkTest`
- `scripts/network-boot.sh` (5047 bytes, testé syntaxe OK)
- `scripts/wake-laptops.sh` (2395 bytes, testé syntaxe OK)
- BACKLOG/TODO/INDEX mis à jour, EPIC 15 à 25%
- Commit + push : `c078a7f` (Gradle tasks) + session 081

== A faire (Session 082)

. US-4 : Hook chroot SSH (`config/live-build/hooks/`) + autorisation clé
. US-2 : Compilation iPXE + script embarqué (`build/undionly.kpxe`)
. US-6 : Test QEMU 2 clients PXE + doc NETWORK_BOOT.adoc
. Boot physique clé USB pour valider V-1

== Rappels

. ZERO commit/push sans permission
. Remote : cccp-education/magic-stick
. Clé USB : /dev/sda, 5 partitions, System A=v0.1.14, System B=vide
. sudo password dans `mdp.txt` (`issa`)
